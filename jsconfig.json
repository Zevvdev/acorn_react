// jsconfig.json (프로젝트 루트에 위치)

{
  "compilerOptions": {
    "baseUrl": "src",         // src 기준으로 import
    "paths": {
      "*": ["*", "src/*"]     // src 하위 모든 폴더 인식
    }
  },
  "include": ["src"]
}